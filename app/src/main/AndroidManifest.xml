<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.klz.lock">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_lock_red"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_lock_red"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.RegistroUsuarioActivity" />
        <activity android:name=".activities.EscolhaActivity" />
        <activity android:name=".activities.RegistroDadosActivity" />
        <activity android:name=".activities.ConsultaDadosActivity"></activity>
    </application>

</manifest>